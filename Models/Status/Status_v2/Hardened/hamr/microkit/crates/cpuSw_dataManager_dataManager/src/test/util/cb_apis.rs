// Do not edit this file as it will be overwritten if codegen is rerun

use data::*;

use proptest::prelude::*;

use super::test_apis::*;

use crate::bridge::cpuSw_dataManager_dataManager_GUMBOX as GUMBOX;

pub enum HarnessResult {
  RejectedPrecondition,
  FailedPostcondition(TestCaseError),
  Passed,
}

/** Contract-based test harness for the initialize entry point
  */
pub fn testInitializeCB() -> HarnessResult
{
  // [InvokeEntryPoint]: Invoke the entry point
  crate::cpuSw_dataManager_dataManager_initialize();

  // [RetrieveOutState]: retrieve values of the output ports via get operations and GUMBO declared local state variable
  let cache = get_cache();
  let zeroize_cmd = get_zeroize_cmd();
  let api_response_log = get_response_log();

  // [CheckPost]: invoke the oracle function
  if !GUMBOX::initialize_IEP_Post (cache, zeroize_cmd, api_response_log) {
    return HarnessResult::FailedPostcondition(
      TestCaseError::Fail("Postcondition failed: incorrect output behavior".into())
    );
  }

  return HarnessResult::Passed
}

#[macro_export]
macro_rules!
testInitializeCB_macro {
  (
    $test_name: ident,
    config: $config:expr
  ) => {
    proptest!{
      #![proptest_config($config)]
      #[test]
      #[serial]
      fn $test_name(empty in ::proptest::strategy::Just(())) {
        match $crate::test::util::cb_apis::testInitializeCB() {
          $crate::test::util::cb_apis::HarnessResult::RejectedPrecondition => {
            unreachable!("This branch is infeasible")
          }
          $crate::test::util::cb_apis::HarnessResult::FailedPostcondition(e) => {
            return Err(e)
          }
          $crate::test::util::cb_apis::HarnessResult::Passed => { }
        }
      }
    }
  };
}

/** Contract-based test harness for the compute entry point
  *
  * @param api_zeroize incoming event port
  * @param api_HMD_log incoming event data port
  * @param api_request_log incoming event data port
  */
pub fn testComputeCB(
  api_zeroize: Option<u8>,
  api_HMD_log: Option<Common::Log_Impl>,
  api_request_log: Option<Common::Request_Impl>) -> HarnessResult
{
  // Initialize the app
  crate::cpuSw_dataManager_dataManager_initialize();

  // [SaveInLocal]: retrieve and save the current (input) values of GUMBO-declared local state variables as retrieved
  //                from the component state
  let In_cache: Common::LogArray_Impl = get_cache();
  let In_zeroize_cmd: bool = get_zeroize_cmd();

  // [PutInPorts]: Set values on the input ports
  put_zeroize(api_zeroize);
  put_HMD_log(api_HMD_log);
  put_request_log(api_request_log);

  // [InvokeEntryPoint]: Invoke the entry point
  crate::cpuSw_dataManager_dataManager_timeTriggered();

  // [RetrieveOutState]: retrieve values of the output ports via get operations and GUMBO declared local state variable
  let cache = get_cache();
  let zeroize_cmd = get_zeroize_cmd();
  let api_response_log = get_response_log();

  // [CheckPost]: invoke the oracle function
  if !GUMBOX::compute_CEP_Post(In_cache, In_zeroize_cmd, cache, zeroize_cmd, api_zeroize, api_HMD_log, api_request_log, api_response_log) {
    return HarnessResult::FailedPostcondition(TestCaseError::Fail("Postcondition failed: incorrect output behavior".into()));
  }

  return HarnessResult::Passed
}

/** Contract-based test harness for the compute entry point
  */
pub fn testComputeCB_container(container: PreStateContainer) -> HarnessResult
{
  return testComputeCB(container.api_zeroize, container.api_HMD_log, container.api_request_log)
}

#[macro_export]
macro_rules!
testComputeCB_macro {
  (
    $test_name: ident,
    config: $config:expr,
    api_zeroize: $api_zeroize_strat:expr,
    api_HMD_log: $api_HMD_log_strat:expr,
    api_request_log: $api_request_log_strat:expr
  ) => {
    proptest!{
      #![proptest_config($config)]
      #[test]
      #[serial]
      fn $test_name(
        (api_zeroize, api_HMD_log, api_request_log)
            in ($api_zeroize_strat, $api_HMD_log_strat, $api_request_log_strat)
      ) {
        match$crate::test::util::cb_apis::testComputeCB(api_zeroize, api_HMD_log, api_request_log) {
          $crate::test::util::cb_apis::HarnessResult::RejectedPrecondition => {
            return Err(proptest::test_runner::TestCaseError::reject(
              "Precondition failed: invalid input combination",
            ))
          }
          $crate::test::util::cb_apis::HarnessResult::FailedPostcondition(e) => {
            return Err(e)
          }
          $crate::test::util::cb_apis::HarnessResult::Passed => { }
        }
      }
    }
  };
}

/** Contract-based test harness for the compute entry point
  *
  * @param In_cache pre-state state variable
  * @param In_zeroize_cmd pre-state state variable
  * @param api_zeroize incoming event port
  * @param api_HMD_log incoming event data port
  * @param api_request_log incoming event data port
  */
pub fn testComputeCBwGSV(
  In_cache: Common::LogArray_Impl,
  In_zeroize_cmd: bool,
  api_zeroize: Option<u8>,
  api_HMD_log: Option<Common::Log_Impl>,
  api_request_log: Option<Common::Request_Impl>) -> HarnessResult
{
  // Initialize the app
  crate::cpuSw_dataManager_dataManager_initialize();

  // [PutInPorts]: Set values on the input ports
  put_zeroize(api_zeroize);
  put_HMD_log(api_HMD_log);
  put_request_log(api_request_log);

  // [SetInStateVars]: set the pre-state values of state variables
  put_cache(In_cache);
  put_zeroize_cmd(In_zeroize_cmd);

  // [InvokeEntryPoint]: Invoke the entry point
  crate::cpuSw_dataManager_dataManager_timeTriggered();

  // [RetrieveOutState]: retrieve values of the output ports via get operations and GUMBO declared local state variable
  let cache = get_cache();
  let zeroize_cmd = get_zeroize_cmd();
  let api_response_log = get_response_log();

  // [CheckPost]: invoke the oracle function
  if !GUMBOX::compute_CEP_Post(In_cache, In_zeroize_cmd, cache, zeroize_cmd, api_zeroize, api_HMD_log, api_request_log, api_response_log) {
    return HarnessResult::FailedPostcondition(TestCaseError::Fail("Postcondition failed: incorrect output behavior".into()));
  }

  return HarnessResult::Passed
}

/** Contract-based test harness for the compute entry point
  */
pub fn testComputeCBwGSV_container(container: PreStateContainer_wGSV) -> HarnessResult
{
  return testComputeCBwGSV(container.In_cache, container.In_zeroize_cmd, container.api_zeroize, container.api_HMD_log, container.api_request_log)
}

#[macro_export]
macro_rules!
testComputeCBwGSV_macro {
  (
    $test_name: ident,
    config: $config:expr,
    In_cache: $In_cache_strat:expr,
    In_zeroize_cmd: $In_zeroize_cmd_strat:expr,
    api_zeroize: $api_zeroize_strat:expr,
    api_HMD_log: $api_HMD_log_strat:expr,
    api_request_log: $api_request_log_strat:expr
  ) => {
    proptest!{
      #![proptest_config($config)]
      #[test]
      #[serial]
      fn $test_name(
        (In_cache, In_zeroize_cmd, api_zeroize, api_HMD_log, api_request_log)
            in ($In_cache_strat, $In_zeroize_cmd_strat, $api_zeroize_strat, $api_HMD_log_strat, $api_request_log_strat)
      ) {
        match $crate::test::util::cb_apis::testComputeCBwGSV(In_cache, In_zeroize_cmd, api_zeroize, api_HMD_log, api_request_log) {
          $crate::test::util::cb_apis::HarnessResult::RejectedPrecondition => {
            return Err(proptest::test_runner::TestCaseError::reject(
              "Precondition failed: invalid input combination",
            ))
          }
          $crate::test::util::cb_apis::HarnessResult::FailedPostcondition(e) => {
            return Err(e)
          }
          $crate::test::util::cb_apis::HarnessResult::Passed => { }
        }
      }
    }
  };
}
