// Do not edit this file as it will be overwritten if HAMR codegen is rerun

//! Property datamanager_zeroize_payload -- Init-State VC: the initial state (all initialize + integration guarantees) satisfies the START assertion.
//! Each proof fn has an empty body: Verus discharges `requires ==> ensures`
//! via SMT; add proof hints in the body if a VC does not discharge.

#![allow(non_snake_case)]
#![allow(unused_variables)]
#![allow(unused_imports)]

use vstd::prelude::*;

use crate::assertions::*;
use crate::datamanager_zeroize_payload::assertions::*;
use crate::contracts::*;
use crate::system_state::SystemState;
use crate::write_frames::*;

verus! {

/** VC[0]: Init-State -- all initialize + integration guarantees |- START */
pub proof fn vc_init_state(st: SystemState)
  requires
    wifi::initialize_defaultAlert(st.alert_cmd),
    data_manager::initialize_defaultZeroize(st.data_manager_zeroize_cmd),
    data_storage::initialize_defaultZeroize(st.data_storage_zeroize_cmd),
    log_monitor::initialize_defaultSince(st.log_monitor_since_result),
    log_monitor::initialize_defaultHistorically(st.log_historically_no_alert),
    report_monitor::initialize_defaultCounterAndSince(st.counter, st.report_monitor_since_result),
  ensures
    true /* START has no assertion */,
{}

} // verus!
