// Do not edit this file as it will be overwritten if HAMR codegen is rerun

use vstd::prelude::*;

use super::*;

verus! {

  #[repr(C)]
  #[derive(Copy, Clone, Debug, PartialEq, Eq)]
  pub enum AnalysisType {
    pub All_Records = 0,
    pub Threat_Only = 1,
    pub Safe_Only = 2,
    pub Unreviewed_Only = 3,
    pub Target_History = 4,
  }

  impl Default for AnalysisType {
    fn default() -> Self
    {
      AnalysisType::All_Records
    }
  }

}