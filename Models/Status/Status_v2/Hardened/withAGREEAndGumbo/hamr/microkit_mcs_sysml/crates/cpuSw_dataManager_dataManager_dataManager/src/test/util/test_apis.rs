// Do not edit this file as it will be overwritten if HAMR codegen is rerun

use crate::bridge::extern_c_api as extern_api;
use data::*;

use proptest::prelude::*;

/// container for component's incoming port values
pub struct PreStateContainer {
  pub api_HMD_log: Option<Common::Log_Impl>,
  pub api_request_log: Option<Common::AnalysisRequest_Impl>,
  pub api_encrypted_response: Option<Common::EncryptedAnalysisResponse_Impl>,
  pub api_zeroize: Option<u8>,
}

/// container for component's incoming port values and GUMBO state variables
pub struct PreStateContainer_wGSV {
  pub In_zeroize_cmd: bool,
  pub api_HMD_log: Option<Common::Log_Impl>,
  pub api_request_log: Option<Common::AnalysisRequest_Impl>,
  pub api_encrypted_response: Option<Common::EncryptedAnalysisResponse_Impl>,
  pub api_zeroize: Option<u8>,
}

/// setter for component's incoming port values
pub fn put_concrete_inputs_container(container: PreStateContainer)
{
  put_HMD_log(container.api_HMD_log);
  put_request_log(container.api_request_log);
  put_encrypted_response(container.api_encrypted_response);
  put_zeroize(container.api_zeroize);
}

/// setter for component's incoming port values and GUMBO state variables
pub fn put_concrete_inputs_container_wGSV(container: PreStateContainer_wGSV)
{
  put_zeroize_cmd(container.In_zeroize_cmd);
  put_HMD_log(container.api_HMD_log);
  put_request_log(container.api_request_log);
  put_encrypted_response(container.api_encrypted_response);
  put_zeroize(container.api_zeroize);
}

/// setter for component's incoming port values
pub fn put_concrete_inputs(
  HMD_log: Option<Common::Log_Impl>,
  request_log: Option<Common::AnalysisRequest_Impl>,
  encrypted_response: Option<Common::EncryptedAnalysisResponse_Impl>,
  zeroize: Option<u8>)
{
  put_HMD_log(HMD_log);
  put_request_log(request_log);
  put_encrypted_response(encrypted_response);
  put_zeroize(zeroize);
}

/// setter for component's incoming port values and GUMBO state variables
pub fn put_concrete_inputs_wGSV(
  In_zeroize_cmd: bool,
  HMD_log: Option<Common::Log_Impl>,
  request_log: Option<Common::AnalysisRequest_Impl>,
  encrypted_response: Option<Common::EncryptedAnalysisResponse_Impl>,
  zeroize: Option<u8>)
{
  put_zeroize_cmd(In_zeroize_cmd);
  put_HMD_log(HMD_log);
  put_request_log(request_log);
  put_encrypted_response(encrypted_response);
  put_zeroize(zeroize);
}

/// setter for IN EventDataPort
pub fn put_HMD_log(value: Option<Common::Log_Impl>)
{
  *extern_api::IN_HMD_log.lock().unwrap_or_else(|e| e.into_inner()) = value
}

/// getter for OUT EventDataPort
pub fn get_encrypted_log() -> Option<Common::StoredLogFile_Impl>
{
  return extern_api::OUT_encrypted_log.lock().unwrap_or_else(|e| e.into_inner()).clone()
}

/// setter for IN EventDataPort
pub fn put_request_log(value: Option<Common::AnalysisRequest_Impl>)
{
  *extern_api::IN_request_log.lock().unwrap_or_else(|e| e.into_inner()) = value
}

/// getter for OUT EventDataPort
pub fn get_storage_request() -> Option<Common::AnalysisRequest_Impl>
{
  return extern_api::OUT_storage_request.lock().unwrap_or_else(|e| e.into_inner()).clone()
}

/// setter for IN EventDataPort
pub fn put_encrypted_response(value: Option<Common::EncryptedAnalysisResponse_Impl>)
{
  *extern_api::IN_encrypted_response.lock().unwrap_or_else(|e| e.into_inner()) = value
}

/// getter for OUT EventDataPort
pub fn get_response_log() -> Option<Common::AnalysisResponse_Impl>
{
  return extern_api::OUT_response_log.lock().unwrap_or_else(|e| e.into_inner()).clone()
}

/// setter for IN EventPort
pub fn put_zeroize(value: Option<u8>)
{
  *extern_api::IN_zeroize.lock().unwrap_or_else(|e| e.into_inner()) = value
}

/// getter for GUMBO State Variable
pub fn get_zeroize_cmd() -> bool
{
  unsafe {
    match &crate::app {
      Some(inner) => inner.zeroize_cmd,
      None => panic!("The app is None")
    }
  }
}

/// setter for GUMBO State Variable
pub fn put_zeroize_cmd(value: bool)
{
  unsafe {
    match &mut crate::app {
      Some(inner) => inner.zeroize_cmd = value,
      None => panic!("The app is None")
    }
  }
}
