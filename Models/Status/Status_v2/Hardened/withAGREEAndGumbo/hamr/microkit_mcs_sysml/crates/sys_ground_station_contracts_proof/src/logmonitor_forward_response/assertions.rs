// Do not edit this file as it will be overwritten if HAMR codegen is rerun

//! Place-assertion spec functions of property `logmonitor_forward_response` -- one per
//! point its decoration binds (unbound points carry `true`, design D5).

#![allow(non_snake_case)]
#![allow(unused_imports)]

use data::*;
use vstd::prelude::*;

use crate::assertions::*;
use crate::system_state::SystemState;

verus! {

/** property logmonitor_forward_response, bound 'after log_monitor' (place after_log_monitor)
  */
pub open spec fn sys_assert_logmonitor_forward_response_after_log_monitor(st: SystemState) -> bool
{
  if (st.data_manager_response.is_some() && st.log_historically_no_alert) {
    st.log_response_out.is_some() &&
      (st.log_response_out.unwrap() == st.data_manager_response.unwrap())
  } else {
    !(st.log_response_out.is_some())
  }
}

} // verus!
