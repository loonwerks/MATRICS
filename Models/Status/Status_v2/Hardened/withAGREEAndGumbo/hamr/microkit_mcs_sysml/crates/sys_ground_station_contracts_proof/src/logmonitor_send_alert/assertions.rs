// Do not edit this file as it will be overwritten if HAMR codegen is rerun

//! Place-assertion spec functions of property `logmonitor_send_alert` -- one per
//! point its decoration binds (unbound points carry `true`, design D5).

#![allow(non_snake_case)]
#![allow(unused_imports)]

use data::*;
use vstd::prelude::*;

use crate::assertions::*;
use crate::system_state::SystemState;

verus! {

/** property logmonitor_send_alert, bound 'after log_monitor' (place after_log_monitor)
  */
pub open spec fn sys_assert_logmonitor_send_alert_after_log_monitor(st: SystemState) -> bool
{
  if (st.data_manager_response.is_some() && !st.log_is_valid) {
    st.log_alert.is_some() && st.storage_alert.is_some()
  } else {
    !(st.log_alert.is_some()) && !(st.storage_alert.is_some())
  }
}

} // verus!
