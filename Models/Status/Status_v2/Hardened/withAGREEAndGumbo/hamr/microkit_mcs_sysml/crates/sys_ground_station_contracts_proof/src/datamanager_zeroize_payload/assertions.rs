// Do not edit this file as it will be overwritten if HAMR codegen is rerun

//! Place-assertion spec functions of property `datamanager_zeroize_payload` -- one per
//! point its decoration binds (unbound points carry `true`, design D5).

#![allow(non_snake_case)]
#![allow(unused_imports)]

use data::*;
use vstd::prelude::*;

use crate::assertions::*;
use crate::system_state::SystemState;

verus! {

/** property datamanager_zeroize_payload, bound 'after data_manager_1' (place after_data_manager_1)
  */
pub open spec fn sys_assert_datamanager_zeroize_payload_after_data_manager_1(st: SystemState) -> bool
{
  (st.data_manager_zeroize_cmd && st.data_manager_request.is_some()) ==>
    IS_ZEROIZED_RESPONSE(st.data_manager_response.unwrap().payload)
}

/** property datamanager_zeroize_payload, bound 'after data_manager_2' (place after_data_manager_2)
  */
pub open spec fn sys_assert_datamanager_zeroize_payload_after_data_manager_2(st: SystemState) -> bool
{
  (st.data_manager_zeroize_cmd && st.data_manager_request.is_some()) ==>
    IS_ZEROIZED_RESPONSE(st.data_manager_response.unwrap().payload)
}

/** property datamanager_zeroize_payload, bound 'after data_manager_3' (place after_data_manager_3)
  */
pub open spec fn sys_assert_datamanager_zeroize_payload_after_data_manager_3(st: SystemState) -> bool
{
  (st.data_manager_zeroize_cmd && st.data_manager_request.is_some()) ==>
    IS_ZEROIZED_RESPONSE(st.data_manager_response.unwrap().payload)
}

} // verus!
